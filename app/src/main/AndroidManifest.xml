<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_vacalogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_vacalogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoControlLeche"
        tools:targetApi="31">
        <activity
            android:name=".ReportePago"
            android:exported="false" />
        <activity
            android:name=".Pagar"
            android:exported="false" />
        <activity
            android:name=".ActualizarProductor"
            android:exported="false" />
        <activity
            android:name=".RegistroNuevoProductor"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProyectoControlLeche.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>